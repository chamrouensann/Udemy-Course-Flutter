import 'package:flutter/material.dart';

void main() {
  var app = MaterialApp(
    title: "Flutter Application",
    debugShowCheckedModeBanner: false,
    home: Scaffold(
      appBar: AppBar(),
    ),
  );
  runApp(app);
}
